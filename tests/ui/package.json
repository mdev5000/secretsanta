{
  "name": "secretsanta-test-ui",
  "version": "0.0.1",
  "scripts": {
    "test": "playwright test --trace on",
    "test:ui": "playwright test --trace on --browser firefox --retries 0 --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.37.0"
  },
  "type": "module",
  "dependencies": {
    "axios": "^1.4.0"
  }
}
