{
  "name": "secretsanta-test-ui",
  "version": "0.0.1",
  "scripts": {
    "test": "playwright test --trace on",
    "test:ui": "playwright test --trace on --browser firefox --retries 0 --ui",
    "report": "playwright show-report",
    "playwright:version": "npm list --depth=0 | grep playwright | node ./scripts/playwright-version.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.37.1"
  },
  "type": "module",
  "dependencies": {
    "axios": "^1.5.0"
  }
}
